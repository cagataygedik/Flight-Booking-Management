/**
 * Main entry point for the Flight Booking Management System.
 * This delegates to the actual implementation in the ui package.
 */
public class Main {
    /**
     * Main method that starts the application.
     * @param args Command line arguments (not used)
     */
    public static void main(String[] args) {
        ui.FlightBookingApp.main(args);
    }
}
